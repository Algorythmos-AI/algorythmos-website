# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.astro
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/video files/
.vercel

# Environment
.env
.env.local
.env.*.local

# Test artifacts
coverage/
test-results/
playwright-report/
.cache/
.playwright-mcp/

# Business / client documents — never commit (contains invoices, bank details, client PII)
Clients/

# Corporate / statutory records — never commit (ASIC keys, ACN/ABN paperwork, signed consents)
asic/
apple-developer/

# Loose commercial documents at repo root (SOWs, signed quotes, client logos)
SOW*
~$*
Integrant-Logo-*.png

# Virtual office provider records (invoices, card details, correspondence)
Bustle-studios/

# Company bank correspondence — never commit (account confirmations, statements)
business-bank-account/

# Supplier bills / receipts — never commit
bills/

# Local tooling and orientation notes — kept locally, never tracked
CLAUDE.md
src/CLAUDE.md
.claude/
