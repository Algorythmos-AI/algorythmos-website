---
import LegalNoticePage from '@/components/pages/LegalNoticePage.astro';
import { PREFIXED_LOCALES } from '@/i18n';

export function getStaticPaths() {
  return PREFIXED_LOCALES.map((locale) => ({ params: { locale } }));
}
---

<LegalNoticePage />
